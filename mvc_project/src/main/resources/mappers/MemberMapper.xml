<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mvc_project.member.mapper.MemberMapper">

    <!-- 회원가입 -->
    <insert id="join" parameterType="MemberDTO">
        INSERT INTO member (
            email, pwd, name, gender, birthday,
            hp, zipcode, addr1, addr2, regdate, state
        ) VALUES (
            #{email}, MD5(#{pwd}), #{name}, #{gender}, #{birthday},
            #{hp}, #{zipcode}, #{addr1}, #{addr2}, NOW(), #{state}
        )
    </insert>

    <!-- 이메일 중복확인 -->
    <select id="emailCheck" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM member WHERE email = #{email}
    </select>

    <!-- 로그인 -->
    <select id="login" parameterType="MemberDTO"
            resultType="MemberDTO">
        SELECT * FROM member
        WHERE email = #{email} AND pwd = MD5(#{pwd})
    </select>

</mapper>