spring:
  application:
    name: chapter14

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/springstudy
    username: testuser
    password: test1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  profiles:
    include: oauth

logging:
  level:
    com.example.chapter14: DEBUG
    org.springframework.security.web: TRACE

file:
  upload-dir: ./upload/board
