<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chapter09.mappers.MemberMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="mId">
        INSERT INTO member (m_name, m_email, m_age)
        VALUES (#{mName}, #{mEmail}, #{mAge})
    </insert>

    <select id="findById" resultType="Member">
        SELECT * FROM member WHERE m_id = #{mId}
    </select>

    <update id="update">
        UPDATE member
        SET m_name = #{mName}, m_email = #{mEmail}, m_age = #{mAge}
        WHERE m_id = #{mId}
    </update>

    <delete id="delete">
        DELETE FROM member WHERE m_id = #{mId}
    </delete>

    <select id="findAll" resultType="Member">
        SELECT * FROM member ORDER BY m_id DESC
    </select>

    <insert id="insertWithKey">
        INSERT INTO member (m_name, m_email, m_age)
        VALUES (#{mName}, #{mEmail}, #{mAge})
        <selectKey keyProperty="mId" resultType="long" order="AFTER">
            SELECT LAST_INSERT_ID();
        </selectKey>
    </insert>

</mapper>