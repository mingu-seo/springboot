spring:
  application:
    name: chapter15

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/springstudy
    username: testuser
    password: test1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.example.chapter15: DEBUG
    org.springframework.security.web: TRACE

jwt:
  member:
    secret: shinhanswacademyshinhanswacademy
    access-exp-min: 30
    refresh-exp-min: 1440
  client:
    secret: shinhanclientsecretshinhanclient
    exp-min: 60